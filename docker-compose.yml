services:
  goproxy:
    build: .
    image: goproxy-geo:local

    container_name: ${CONTAINER_NAME:-goproxy}
    restart: unless-stopped
    ports:
      - "${WEBUI_PORT:-7800}:7800"     # WebUI
      - "${SOCKS5_PORT:-7801}:7801"   # SOCKS5 proxy
      - "${HTTP_PORT:-7802}:7802"     # HTTP proxy
    volumes:
      - goproxy-data:/app/data
    networks:
      - goproxy-net
    environment:
      - TZ=${TZ:-Asia/Shanghai}
      - DATA_DIR=/app/data
      # 登录凭据（WebUI 密码、代理账号/密码）在首次启动时自动生成并打印到日志，
      # 存入 config.json；此处不再通过环境变量注入。用 docker compose logs 查看首次凭据。
      - SESSION_TTL_MINUTES=${SESSION_TTL_MINUTES:-10}
      - DEFAULT_REGION=${DEFAULT_REGION}
      - ALLOWED_COUNTRIES=${ALLOWED_COUNTRIES}
      - BLOCKED_COUNTRIES=${BLOCKED_COUNTRIES}
      - HEALTH_CHECK_INTERVAL=${HEALTH_CHECK_INTERVAL:-5}
      - MAX_RETRY=${MAX_RETRY:-3}
      - SINGBOX_PATH=${SINGBOX_PATH:-sing-box}
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:7800/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  goproxy-data:

networks:
  goproxy-net:
    name: goproxy-net
    driver: bridge
